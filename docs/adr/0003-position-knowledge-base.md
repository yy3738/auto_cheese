# "学习"定义为查询式跨对局知识库，不在端上训练模型

用户明确要求产品"一定要有自我学习与提升"。端上更新神经网络权重需要自我对弈产生海量局面数据，手机不可行，且出厂引擎已近理论上限，结果必然变弱。采纳的唯一学习机制：实战中缓存轮到用户走前的局面 → 对局结束后后台深度复盘（movetime 3000ms）→ 以 Zobrist 哈希为主键写入 Room 数据库（局面、最佳走法、评分、深度、出现次数、用户选择、实际胜负）→ 下次实战先查库，命中即秒回且不差于上次。对手建模（单局内风格适应）否决：会偏离引擎最优解，实战辅助场景下是负收益。
