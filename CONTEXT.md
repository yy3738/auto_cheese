# 中国象棋实战助手（Android 悬浮窗）

个人自用的 Android 实战辅助 App：截屏识别棋局，本地引擎给出带评分的推荐走法，并通过跨对局知识库持续提升推荐质量与用户棋力。不上架商店。

## Language

### 核心概念

**实战辅助**：
在用户对弈进行中实时识别局面并给出走法建议的运行模式。
_Avoid_: 作弊器、外挂

**有限深度搜索**：
引擎在固定时间预算内向后推演若干步、返回当前最优走法的能力。这是本产品的核心能力定义，"深度几步"由时间预算自然决定，而非硬性步数上限。
_Avoid_: 几步以内判断、穷举

**候选走法（Multi-PV）**：
同一局面下按引擎评分排序的前 3 条走法及其分差，用于让用户理解"为什么"，而非只知"是什么"。

**最佳走法（bestmove）**：
引擎在当前时间预算内给出的评分最高的走法，是候选走法中的第一条。

### 识别相关

**校准**：
首次对接某个目标 App 时的一次性流程：用户截取棋盘图并框定范围，App 切出 9×10 网格并采集 14 类棋子模板。更换棋子皮肤或目标 App 时需重新校准。
_Avoid_: 训练、适配

**棋子模板**：
校准产生的 14 个棋子图像样本（红黑 × 车马炮兵将士象帅），是运行时识别的比对基准。

**局面矩阵**：
一次识别的输出——90 个交叉点上各自的棋子归属与类型，是交给引擎的输入。

**箭头层**：
可选开启的全屏透明悬浮层，触摸穿透，直接在目标 App 棋盘上绘制候选走法箭头（红=最佳，黄=次优）。
_Avoid_: 全屏覆盖层

**信息条**：
常驻的可拖动小型悬浮窗，显示候选走法、评分与搜索深度，不用时可缩为圆点。

### 学习相关

**局面知识库**：
带胜负权重的本地局面数据库：记录局面、已验证的最佳走法、评分、深度、出现次数、用户实际选择与对局结果。同一局面第二次遇到的推荐不差于第一次——这是本产品"自我提升"一词的唯一含义。
_Avoid_: 神经网络更新、模型训练、自我学习

**复盘**：
对局结束后对关键局面的深度重新分析，产出评分曲线与坏棋标记，是写入局面知识库的数据来源，也是用户提升棋力的主要途径。

**坏棋标记**：
复盘中对用户走法与最佳走法的分差标注：≥1.5 分为"失误"，≥3 分为"大漏着"。
_Avoid_: 错误、失误（作为无阈值泛称使用时）

### 走法表示

**纵线格式**：
面向用户的中文走法记法（如"炮二平五"）。
_Avoid_: 中文记法、传统记法

**ICCS 坐标**：
面向机器的走法记法（如 h2e2），用于引擎通信与知识库存储。
